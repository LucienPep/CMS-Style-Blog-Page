<div class="text-center post-div">
    <form class="form update-post-form">
        <div class="form-group">
          <label for="post-title">Title:</label>
          <textarea class="form-input" type="text" id="post-title" name="post-name">{{title}}</textarea>
          <label for="post-title">Content:</label>
          <textarea class="form-input" id="post-content" name="post-content">{{content}}</textarea>
          <p>Created by {{user.name}} on {{format_date date_created}}</p>
        </div>
          <button id="{{id}}" type="submit" class="btn btn-primary">Update</button>
    </form>
</div>

<script src="/js/profile.js"></script>
